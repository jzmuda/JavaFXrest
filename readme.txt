Java FX module to view books from a remote repository with REST requests.
The repository code is included in a SpringMVC folder (separate project).
The default server should be Apache 7.0 and default URL to enter is http://localhost:8080/webstore/
